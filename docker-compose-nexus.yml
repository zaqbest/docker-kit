services:
  nexus:
    image: sonatype/nexus3
    restart: always
    environment:
      - NEXUS_SECURITY_RANDOMPASSWORD=false
      - TZ=Asia/Shanghai
    volumes:
      - ./nexus-data:/nexus-data
    ports:
      - "8081:8081"
      - "8085:8085"
