services:
  elasticsearch:
    image: elasticsearch:8.19.14
    restart: always
    networks:
      - docker-kit-network
    ports:
      - "${ELASTICSEARCH_HTTP_PORT}:9200"
      - "${ELASTICSEARCH_TRANSPORT_PORT}:9300"
    volumes:
      - ./elasticsearch/data:/usr/share/elasticsearch/data
      - ./certs:/usr/share/elasticsearch/config/certs:ro
      - ./elasticsearch/plugins:/usr/share/elasticsearch/plugins:ro
      # 用户词典 / 连字模式等分析资源，analyzer 里通过 config/analysis/xxx 引用
      - ./elasticsearch/config/analysis:/usr/share/elasticsearch/config/analysis:ro
    env_file:
      - env/elasticsearch.env
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536

  kibana:
    image: kibana:8.19.14
    restart: always
    networks:
      - docker-kit-network
    ports:
      - "${KIBANA_PORT}:5601"
    volumes:
      - ./certs:/usr/share/kibana/config/certs:ro
    env_file:
      - env/kibana.env
    depends_on:
      - elasticsearch

networks:
  docker-kit-network:
    external: true
